export * from "./explain";

/*Allows for easy access to all controller functions
Provides a single place to manage what's exported from the controllers folder. */

/* // Instead of this:
import { explainHandler } from "./controllers/explain";

// You can do this:
import { explainHandler } from "./controllers";
*/
